/**
 * MainPreview — основная область 3D-превью
 *
 * Здесь будет R3F-сцена с Three.js-моделью вазы.
 * Пока содержит заглушку.
 */

export function MainPreview() {
  return (
    <main className="main-preview">
      <div className="main-preview__placeholder">3D-превью</div>
      <div className="main-preview__info">w: — | h: — | d: —</div>
    </main>
  );
}
